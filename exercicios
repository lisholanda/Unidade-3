'                                                  Ler um valor e escrever se é positivo, negativo ou zero. 
Ler 3 valores (considere que não informados deles valores iguais) e escreva o maior deles.
Ler 3 valores (considerar que não foram informados sobre valores)  e escrever soma dos 2 maiores.
 

Ler 3 valores (considerar que não informados valores iguais) e ecrevêlos em ordem crescente.
Ler 3 valores (A, B e C) representando como medidas dos lados de um triângulo e escrever se formam ou não um triângulo. OBS: para formar um triângulo, o valor de cada lado deve ser menor que a soma dos outros 2 lados.
Ler o nome de 2 times e o número de gols marcados na partida (para cada time). Escrever o nome do vencedor. Caso não haja vencedor deve ser impressa na palavra EMPATE.
Ler dois valores e imprimir uma das três mensagens a seguir:
'Números iguais', caso os números sejam iguais

'Primeiro é maior', caso o primeiro seja maior que o segundo; 'Segundo maior', caso os

egundo seja maior que o primeiro.  

